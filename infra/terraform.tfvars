# ── Fill these in before running terraform apply ────────────────────────────
project_id  = "pariverse-prod"
region      = "asia-south1"           # Mumbai — closest to India
db_password = "CHANGE_ME_STRONG_PASSWORD"   # Pick a strong password (min 16 chars)
